import type { ToolRegistry } from '@simodelne/pgas-server/plugin.js';

export const registeredToolNames = [] as const;

export function register{{PASCAL_NAME}}Tools(_registry: ToolRegistry): void {
  // The generic skeleton has no tools by default. Synthesized programs add
  // semantic tools only when the intake requires them.
}
